import 'package:flutter/material.dart';

// 앱에서 사용할 색상을 미리 정의합니다.
class AppColors {
  static const Color primary = Color(0xFF6A1B9A); // 진한 보라
  static const Color secondary = Color(0xFFF0EBF8); // 연한 보라 (배경)
  static const Color accent = Color(0xFF00E676); // 밝은 초록 (포인트)
  static const Color textPrimary = Color(0xFF212121); // 기본 텍스트
  static const Color textSecondary = Color(0xFF757575); // 보조 텍스트
  static const Color winBackground = Color(0xFFE0F2F1); // 승리 배경
  static const Color loseBackground = Color(0xFFFFEBEE); // 패배 배경
  static const Color cardBackground = Colors.white; // 이 라인을 추가합니다.
}
